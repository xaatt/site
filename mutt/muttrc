# About Me
set from = "v@xaat.org"
set realname = "Vladyslav"

# My credentials
set smtp_url = "smtps://v@xaat.org@mail.postale.io:465/"
set smtp_pass = 
set imap_pass = 

# My mailboxes
set folder = imaps://v@xaat.org@mail.postale.io:993
set imap_check_subscribed
set spoolfile = +INBOX
set record    = +Sent
set trash = +Trash
set postponed = +Drafts

# Where to put the stuff
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~/.mutt/certificates"

# Etc
set ssl_force_tls = no
set hostname = xaat.org
set move = no
set imap_keepalive = 90
set mail_check = 60 
set timeout = 30 
set sort = threads
set editor = vim
set sort=threads
set abort_nosubject = no
set sort_aux = reverse-last-date-received
set pager_stop
set user_agent = yes
set forward_format = "Fwd: %s"
set delete = yes
set signature = ~/.mutt/signature
set confirmappend = no
set include = yes
set fast_reply
set charset = utf-8
set index_format="%4C %Z %[%d/%m/%Y] %-15.15F (%4c) %s"
unignore Message-ID:

#OpenPGP
set pgp_sign_as = "v@xaat.org"
set pgp_default_key = "C3DCCF3A092DF366"
set pgp_autoinline= no
set crypt_replyencrypt = yes
set crypt_replysignencrypted = yes
set crypt_protected_headers_save = yes
set crypt_protected_headers_write = yes
set crypt_protected_headers_subject = ...
#set crypt_opportunistic_encrypt = yes

#Sidebar
set sidebar_visible = yes
set sidebar_width = 20
set sidebar_folder_indent = yes
set sidebar_short_path = yes
set sidebar_format="%B %> %N/%S"
set mail_check_stats

# Указание папок для отображения
mailboxes +INBOX

# sidebar mappings
bind index,pager \Ck sidebar-prev
bind index,pager \Cj sidebar-next
bind index,pager \Co sidebar-open
bind index,pager \Cp sidebar-prev-new
bind index,pager \Cn sidebar-next-new
bind index,pager \Cb sidebar-toggle-visible
